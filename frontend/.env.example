# src/.env (frontend)
# Vite exposes only vars prefixed with VITE_

# Backend API base URL
# In development, Vite's proxy rewrites /api → http://localhost:5000
# In production, set to your deployed backend URL
VITE_API_URL=/api
